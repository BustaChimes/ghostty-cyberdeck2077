# Screenshots

Drop terminal screenshots here. Add a shot of a full window
(banner + prompt + some glitched output) as `hero.png`, then uncomment the
`![screenshot](screenshots/hero.png)` line near the top of the main README.
